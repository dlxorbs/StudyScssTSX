import react from 'react'

interface Props {
    name?: string | null;
}

function Header({ name }: Props) {
    return (
        <div className='Header'>

        </div>
    );
}

export default Header;
